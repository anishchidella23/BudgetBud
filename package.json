{
  "name": "budgetbud",
  "version": "1.0.0",
  "private": true,
  "description": "A budgeting app for tracking spending, bills, and savings goals.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
